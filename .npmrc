# ===========================================================================
# pnpm configuration for mflocss/starter
# ===========================================================================
#
# pnpm 固有の設定は pnpm v11 以降 `pnpm-workspace.yaml` が正本になりました。
# override と minimum-release-age の除外は [pnpm-workspace.yaml](./pnpm-workspace.yaml) を参照。
#
# このファイルは npm / yarn / pnpm 共通の registry 設定等を置く場所として残しています。
